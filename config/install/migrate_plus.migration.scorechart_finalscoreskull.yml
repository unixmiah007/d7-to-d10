
id: scorechart_finalscoreskull
label: 'Migrate finalscoreskull from Drupal 7 to Drupal 10'
migration_tags:
  - Drupal 7
  - Content
deriver: Drupal\bnc_migrate\Plugin\migrate\BncScorechartDeriver
source:
  plugin: scorechart_field_finalscoreskull
  key: migrate_drupal_7
  fields:
    - entity_type
    - bundle
    - deleted
    - entity_id
    - revision_id
    - langcode
    - delta
    - field_finalscoreskull_length
    - field_finalscoreskull_width
    - field_finalscoreskull_total
process:
  bundle: bundle
  deleted: deleted
  entity_id: entity_id
  entity_type: entity_type
  revision_id: revision_id
  langcode: language
  delta: delta
  field_finalscoreskull_length:
    plugin: concat
    source:
      - finalscoreskull_length
  field_finalscoreskull_width:
    plugin: concat
    source:
      - finalscoreskull_width
  field_finalscoreskull_total:
    plugin: concat
    source:
      - finalscoreskull_total
destination:
  source_module: bnc_migrate
  default_bundle: finalscoreskull
  plugin: scorechart_field_finalscoreskull
  key:
    - entity_id
    - delta
    - langcode
dependencies:
  enforced:
    module:
      - bnc_migrate
