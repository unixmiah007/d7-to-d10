
id: scorechart_numabpoints
label: 'Migrate numabpoints from Drupal 7 to Drupal 10'
migration_tags:
  - Drupal 7
  - Content
deriver: Drupal\bnc_migrate\Plugin\migrate\BncScorechartDeriver
source:
  plugin: scorechart_field_numabpoints
  key: migrate_drupal_7
  fields:
    - entity_type
    - bundle
    - deleted
    - entity_id
    - revision_id
    - langcode
    - delta
    - field_numabpoints_right
    - field_numabpoints_left
    - field_numabpoints_total
process:
  bundle: bundle
  deleted: deleted
  entity_id: entity_id
  entity_type: entity_type
  revision_id: revision_id
  langcode: language
  delta: delta
  field_numabpoints_right:
    plugin: concat
    source:
      - numabpoints_right
  field_numabpoints_left: 
    plugin: concat
    source:
      - numabpoints_left 
  field_numabpoints_total:
    plugin: concat
    source:
      - numabpoints_total
destination:
  source_module: bnc_migrate
  default_bundle: numabpoints
  plugin: scorechart_field_numabpoints
  key:
    - entity_id
    - delta
    - langcode
dependencies:
  enforced:
    module:
      - bnc_migrate
